syntax = "proto3";

package resonator.proto.api;

message Person {
  string id = 1;
  string username = 2;
  // typically will be left unset in API responses, primarily used for account creation
  string email = 3;
}
